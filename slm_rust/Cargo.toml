[package]
name = "slm_rust"
version = "0.1.0"
edition = "2024"
description = "Rust target projection for the Semantic Logic Model prototype"
license = "UNLICENSED"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
