[project]
name = "slm-prototype"
version = "0.1.0"
description = "Prototype Semantic Logic Model reasoning layer"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
